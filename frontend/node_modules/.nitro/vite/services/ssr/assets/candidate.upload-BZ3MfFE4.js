import { t as api } from "./api-BmP1cRwt.js";
import { r as cn, t as Button } from "./button-PwNqyxv_.js";
import { t as PageHeader } from "./portal-shell-Bqe4KT1I.js";
import { i as CardTitle, n as CardContent, r as CardHeader, t as Card } from "./card-Befnk9fX.js";
import { t as Badge } from "./badge-B3f60TId.js";
import { useEffect, useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { toast } from "sonner";
import { CheckCircle2, FileText, Loader2, UploadCloud } from "lucide-react";
//#region src/routes/candidate.upload.tsx?tsr-split=component
function UploadPage() {
	const [file, setFile] = useState(null);
	const [dragging, setDragging] = useState(false);
	const [candidate, setCandidate] = useState(null);
	const [profileError, setProfileError] = useState(null);
	const [isLoadingProfile, setIsLoadingProfile] = useState(true);
	const [isUploading, setIsUploading] = useState(false);
	const currentUser = getStoredUser();
	const loadProfile = async () => {
		if (!currentUser?.id) {
			setCandidate(null);
			setProfileError("Sign in as a candidate to upload a resume.");
			setIsLoadingProfile(false);
			return;
		}
		setIsLoadingProfile(true);
		setProfileError(null);
		try {
			const response = await api.get(`/candidates/${currentUser.id}`);
			setCandidate(response.data);
		} catch (err) {
			console.error("Failed to load candidate profile", err);
			setCandidate(null);
			setProfileError("Unable to load your candidate profile.");
		} finally {
			setIsLoadingProfile(false);
		}
	};
	useEffect(() => {
		loadProfile();
	}, []);
	const onFiles = async (files) => {
		if (!files || !files[0]) return;
		const selectedFile = files[0];
		const extension = selectedFile.name.split(".").pop()?.toLowerCase();
		if (!["pdf", "docx"].includes(extension ?? "")) {
			toast.error("Only PDF and DOCX resumes are allowed");
			return;
		}
		if (!currentUser?.id) {
			toast.error("Sign in as a candidate to upload a resume");
			return;
		}
		const formData = new FormData();
		formData.append("resume", selectedFile);
		formData.append("candidate_id", String(currentUser.id));
		setFile(selectedFile);
		setIsUploading(true);
		try {
			await api.post("/resume/upload", formData, { headers: { "Content-Type": "multipart/form-data" } });
			toast.success("Resume uploaded successfully");
			await loadProfile();
		} catch (err) {
			console.error("Failed to upload resume", err);
			toast.error("Resume upload failed");
		} finally {
			setIsUploading(false);
		}
	};
	return /* @__PURE__ */ jsxs(Fragment, { children: [/* @__PURE__ */ jsx(PageHeader, {
		title: "Upload resume",
		description: "Upload your latest PDF or DOCX resume."
	}), /* @__PURE__ */ jsxs("div", {
		className: "grid gap-6 lg:grid-cols-3",
		children: [
			/* @__PURE__ */ jsx(Card, {
				className: "border-dashed border-border/70 shadow-[var(--shadow-card)] lg:col-span-2",
				children: /* @__PURE__ */ jsx(CardContent, {
					className: "p-0",
					children: /* @__PURE__ */ jsxs("label", {
						onDragOver: (e) => {
							e.preventDefault();
							setDragging(true);
						},
						onDragLeave: () => setDragging(false),
						onDrop: (e) => {
							e.preventDefault();
							setDragging(false);
							onFiles(e.dataTransfer.files);
						},
						className: cn("flex cursor-pointer flex-col items-center justify-center gap-3 rounded-xl px-8 py-20 text-center transition", dragging && "bg-primary/5", isUploading && "pointer-events-none opacity-70"),
						children: [
							/* @__PURE__ */ jsx("div", {
								className: "grid h-14 w-14 place-items-center rounded-full bg-primary/10 text-primary",
								children: isUploading ? /* @__PURE__ */ jsx(Loader2, { className: "h-6 w-6 animate-spin" }) : /* @__PURE__ */ jsx(UploadCloud, { className: "h-6 w-6" })
							}),
							/* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("div", {
								className: "text-base font-semibold",
								children: isUploading ? "Uploading resume..." : "Drag & drop your resume"
							}), /* @__PURE__ */ jsx("p", {
								className: "mt-1 text-sm text-muted-foreground",
								children: "PDF or DOCX"
							})] }),
							/* @__PURE__ */ jsx(Button, {
								asChild: true,
								size: "sm",
								disabled: isUploading,
								children: /* @__PURE__ */ jsx("span", { children: "Browse files" })
							}),
							/* @__PURE__ */ jsx("input", {
								type: "file",
								accept: ".pdf,.docx",
								className: "hidden",
								disabled: isUploading,
								onChange: (e) => void onFiles(e.target.files)
							})
						]
					})
				})
			}),
			/* @__PURE__ */ jsxs(Card, {
				className: "border-border/60 shadow-[var(--shadow-card)]",
				children: [/* @__PURE__ */ jsx(CardHeader, {
					className: "pb-2",
					children: /* @__PURE__ */ jsx(CardTitle, {
						className: "text-sm font-semibold",
						children: "Candidate profile"
					})
				}), /* @__PURE__ */ jsx(CardContent, {
					className: "space-y-3",
					children: isLoadingProfile ? /* @__PURE__ */ jsxs("div", {
						className: "flex items-center gap-2 text-sm text-muted-foreground",
						children: [/* @__PURE__ */ jsx(Loader2, { className: "h-4 w-4 animate-spin" }), "Loading profile..."]
					}) : profileError ? /* @__PURE__ */ jsxs("div", {
						className: "space-y-3",
						children: [/* @__PURE__ */ jsx("p", {
							className: "text-sm text-destructive",
							children: profileError
						}), /* @__PURE__ */ jsx(Button, {
							variant: "outline",
							size: "sm",
							onClick: () => void loadProfile(),
							children: "Retry"
						})]
					}) : candidate ? /* @__PURE__ */ jsxs(Fragment, { children: [
						/* @__PURE__ */ jsx(InfoRow, {
							label: "Name",
							value: candidate.name
						}),
						/* @__PURE__ */ jsx(InfoRow, {
							label: "Email",
							value: candidate.email
						}),
						/* @__PURE__ */ jsx(InfoRow, {
							label: "Role",
							value: candidate.role
						}),
						/* @__PURE__ */ jsx(InfoRow, {
							label: "Candidate ID",
							value: String(candidate.id)
						})
					] }) : null
				})]
			}),
			/* @__PURE__ */ jsxs(Card, {
				className: "border-border/60 shadow-[var(--shadow-card)] lg:col-span-3",
				children: [/* @__PURE__ */ jsxs(CardHeader, {
					className: "flex flex-row items-center justify-between space-y-0 pb-3",
					children: [/* @__PURE__ */ jsxs(CardTitle, {
						className: "flex items-center gap-2 text-sm font-semibold",
						children: [/* @__PURE__ */ jsx(FileText, { className: "h-4 w-4 text-primary" }), " Saved resume"]
					}), candidate?.resumes.length ? /* @__PURE__ */ jsx(Badge, {
						variant: "secondary",
						className: "rounded-full",
						children: "Stored in PostgreSQL"
					}) : null]
				}), /* @__PURE__ */ jsx(CardContent, { children: candidate?.resumes.length ? /* @__PURE__ */ jsxs("div", {
					className: "rounded-lg border border-border/70 bg-muted/40 p-4",
					children: [/* @__PURE__ */ jsxs("div", {
						className: "mb-2 flex min-w-0 items-center gap-2 text-sm",
						children: [/* @__PURE__ */ jsx(CheckCircle2, { className: "h-4 w-4 shrink-0 text-success" }), /* @__PURE__ */ jsx("span", {
							className: "truncate font-medium",
							children: fileNameFromPath(candidate.resumes[0].resume_path)
						})]
					}), /* @__PURE__ */ jsx("div", {
						className: "truncate text-xs text-muted-foreground",
						children: candidate.resumes[0].resume_path
					})]
				}) : file ? /* @__PURE__ */ jsx("div", {
					className: "rounded-lg border border-border/70 bg-muted/40 p-4 text-sm text-muted-foreground",
					children: isUploading ? `Uploading ${file.name}...` : "Upload finished, refreshing profile..."
				}) : /* @__PURE__ */ jsx("p", {
					className: "text-sm text-muted-foreground",
					children: "No resume has been uploaded yet."
				}) })]
			})
		]
	})] });
}
function InfoRow({ label, value }) {
	return /* @__PURE__ */ jsxs("div", {
		className: "flex items-center justify-between gap-4 border-b border-border/70 pb-2 text-sm last:border-0 last:pb-0",
		children: [/* @__PURE__ */ jsx("span", {
			className: "text-muted-foreground",
			children: label
		}), /* @__PURE__ */ jsx("span", {
			className: "min-w-0 truncate font-medium",
			children: value
		})]
	});
}
function getStoredUser() {
	if (typeof window === "undefined") return null;
	const value = localStorage.getItem("user");
	if (!value) return null;
	try {
		return JSON.parse(value);
	} catch {
		return null;
	}
}
function fileNameFromPath(path) {
	return path.split(/[\\/]/).pop() || path;
}
//#endregion
export { UploadPage as component };
